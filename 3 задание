 def f(x):
    k=2
    deliteli=set()     // создание пустого множества `deliteli` для хранения делителей числа.
    while k*k<=x:
        if x%k==0:          //проверка условия: если x делится на k без остатка.
            deliteli.add(k)
            if x//k<x:              // проверка условия: если делитель x на k меньше x.
                deliteli.add(x//k)
        k=k+1
    return sorted(deliteli)
start=245690
end=245757
n=0
for i in range(start,end+1):
    n=n+1
    if len(f(i))==0:
        print(n,i)
